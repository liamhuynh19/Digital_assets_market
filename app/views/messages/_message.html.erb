<div class="message mb-3 <%= message.user_id == current_user.id ? 'text-end' : '' %>">
  <div class="d-inline-block p-3 rounded <%= message.user_id == current_user.id ? 'bg-primary text-white' : 'bg-light' %>" 
       style="max-width: 80%;">
    <div class="small mb-1 <%= message.user_id == current_user.id ? 'text-white-50' : 'text-muted' %>">
      <%= message.user.email %> • <%= time_ago_in_words(message.created_at) %> ago
    </div>
    <div><%= message.content %></div>
</div>
</div>