<h1>Apply to Become a Seller</h1>

<p>As a seller, you'll be able to selling your digital products on our platform.</p>

<%= form_with(model: @seller_application, local: true) do |form| %>
  <% if @seller_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seller_application.errors.count, "error") %> prohibited this application from being saved:</h2>

      <ul>
        <% @seller_application.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reason, "Tell us why you want to become a seller" %>
    <%= form.text_area :reason, rows: 5, class: "form-control", required: true %>
    <small class="form-text text-muted">Please provide details about what kind of digital products you plan to sell and your background in this area.</small>
  </div>

  <div class="actions mt-4">
    <%= form.submit "Submit Application", class: "btn btn-primary" %>
  </div>
<% end %>

<div class="mt-3">
  <%= link_to "Back", root_path %>
</div>
